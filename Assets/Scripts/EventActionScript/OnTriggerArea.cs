using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class OnTriggerArea : MonoBehaviour
{
    //public int id;

    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.CompareTag("Ledge Checker"))
    //    {
    //        Debug.Log("collided with ledge : " + other.CompareTag("Ledge Checker"));
    //        //GrabLedge(LedgeGrab.instance.newHandPos);
    //        GameManager.instance.LedgeTriggerEnter(id);
    //    }
    //}

    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Space) && LedgeGrab.instance._grabbedLedge)
    //    {
    //        LedgeGrab.instance._grabbedLedge = false;
    //        transform.position = LedgeGrab.instance.GetStandUpPos();
    //        GameManager.instance.playerMovement.enabled = true;
    //        GameManager.instance.playerMovement.rb.isKinematic = false;

    //        //GameManager.instance.ClimbFromTheLedge();

    //        //Debug.Log(Input.GetKeyDown(KeyCode.Space) && LedgeGrab.instance._grabbedLedge);
    //    }
    //}
}
